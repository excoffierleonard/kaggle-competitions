services:
  app:
    platform: linux/amd64
    build:
      context: .
      args:
        - KAGGLE_API_TOKEN=${KAGGLE_API_TOKEN}
    image: excoffierleonard/kagglecomp-pgs06e01:latest
    environment:
      KAGGLE_API_TOKEN: ${KAGGLE_API_TOKEN}
      PRESETS: ${PRESETS:-}
      TIME_LIMIT: ${TIME_LIMIT:-}
      SUBMIT: ${SUBMIT:-}